# This file is automatically loaded by Docker Compose and customizes the base configuration
services:
  app:
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload